# fix_door_wall_render: implementation plan (door replacement breaks wall rendering)

Spec — [spec.md](./spec.md); research — [research.md](./research.md).

**Format:** each stage = TDD (red test → green implementation → refactor) + a commit at the end.
This file is a **living progress journal** — update statuses as work proceeds.

**Test-adapter note:** the repo has no automated unit-test harness and the game cannot be run
in this sandbox. A stage's "test" is therefore: (a) a static check (shell script in `./.tmp/`,
kept out of the build) asserting observable facts about the code, and (b) a code-level trace of
the game flow against `/home/apkawa/code/ONI_MODS/lib_sources/Assembly-CSharp` performed by an
independent agent. "Red" = the trace/check identifies the failing behavior line-by-line;
"green" = implementation + static check + `dotnet build ONI-mods.sln -c Debug` pass.
Final in-game verification is a manual step for the user (checklist in Stage 4).

Legend: `[ ]` not started · `[~]` in progress · `[x]` done.

---

## Stage 0 — Research + spec + plan
- [x] Superficial research (ralph, 2 rounds: round 2 re-verified all citations) → `research.md` §1–§8 (mod patches, door-def diff table, game flow end-to-end, BlockTileRenderer mechanics, cancel path, log inspection)
- [x] Refine spec.md v2 (root cause: IsTilePiece ⇒ Constructable.MarkArea overwrites layer-9 of the live wall cell; cancel never restores it; completion self-heals; LogicPorts ruled out)
- [x] Write plan.md

**Criterion:** `draft.md`, `research.md`, `spec.md`, `plan.md` exist in the task dir; spec open questions
reduced to 4 deep-trace items; no research finding left unwritten.
**Commit:** `<type>(<scope>): <summary>`

## Stage 1 — Red: deep trace of completion in the broken orientation + fix mechanics verification
- [x] Trace (a) — what actually happens at construction COMPLETION in the broken orientation (anchor in air, wall in upper cell, plan exists in `Grid.Objects[upperWallCell, 9]` instead of the wall): does the plan GO carry a `BuildingComplete` component and which KPrefabID tags does the UnderConstruction prefab have? What does `GetReplacementCandidate(upperWallCell)` in `Constructable.OnCompleteWork`/`FinishConstruction` (Constructable.cs:124-291) actually return — the wall or the plan GO? Do the plan GO's gates (`Def.Replaceable`, `CanReplace`) pass? Conclude: is the wall destroyed exactly once, not at all, or is there a double-handling — i.e. is there a HIDDEN second bug beyond rendering?
- [x] Trace (b) — fix mechanics: confirm `Constructable.MarkArea` runs at spawn (`OnSpawn` :345) inside `Instantiate` inside `def.TryReplaceTile` (BuildingDef.cs:529-540) — i.e. the layer-9 overwrite is complete BEFORE `TryReplaceTile` returns, so restore-after-return is the correct window; confirm `TileVisualizer.RefreshCell` → `RefreshCellInternal` (TileVisualizer.cs:5-21) → `BlockTileRenderer.Rebuild(layer, cell)` (:775-784) re-dirties the chunk and `GetConnectionBits` (:581-628) then reads the restored live wall GO (connection bits correct → no insets); confirm nothing else between plan creation and completion reads `Grid.Objects[upperWallCell, 9]` in a way that would break with the wall (not the plan) restored there.
- [x] Trace (c) — consumers of `Grid.IsTileUnderConstruction[anchor]=true` (set at plan spawn; compare with the native flow where the same happens) and a short save/load note (informational; the corrupted state disappears after the fix).
- [x] Record everything with file:line citations in `.tmp/fix_door_wall_render_trace.md`; state explicitly whether the Stage-2 restore design (spec "Подход") needs any adjustment based on (a)–(c).

**Stage 1 outcome (journal):** trace delivered (245 lines, `.tmp/fix_door_wall_render_trace.md`). **(a) = option (b): the wall is NOT destroyed at all in the broken orientation — a hidden second bug.** The plan GO lacks `BuildingComplete` (BuildingLoader.cs:173-244) while sitting in both layer-9 slots → gated `GetReplacementCandidate` (BuildingDef.cs:326-338, gate :333) returns null at anchor AND upper cell → the destroy/refund/trigger blocks never run → live wall orphaned under the completed door (two PrimaryElements in one sim cell, wall mesh keeps rendering inside the door). Native orientations: wall destroyed exactly once (reference §A.4). **(b):** clobber completes synchronously inside `TryReplaceTile` (§B.1) → capture-before/restore-after window exact; one `TileVisualizer.RefreshCell(upper, FoundationTile, ReplacementTile)` repairs rendering (4-neighbor sweep covers anchor, §B.2). **(c):** fixed grid state `{anchor: plan, upper: wall}` is behaviorally identical to the native "plan adjacent to wall" state for all ~25 layer-9 readers (§B.3); `IsTileUnderConstruction` flag = native behavior for an air anchor (§B.4); mid-construction save/load nondeterminism is a PRE-EXISTING property of `Constructable.OnSpawn` (wall's OnSpawn vs plan's OnSpawn order), unaffected by the fix (§B.5). **Design confirmed, no structural change** (trace §D): capture per area cell before `def.TryReplaceTile`, restore non-null captures after `plan != null`, keep the plan in the anchor slot, one RefreshCell per restored cell, skip when `def.TileLayer == ObjectLayer.NumLayers`. Intended behavioral note: post-fix completion destroys + refunds the wall once (previously orphaned).

**Criterion:** `.tmp/fix_door_wall_render_trace.md` answers (a) with a definitive destruction-count conclusion, (b) line-by-line with no unverified assumption, (c) consumers listed; design confirmed or adjusted with justification.
**Commit:** N/A (trace doc lives in .tmp; spec/plan updates ride with the Stage 2 commit)

## Stage 2 — Green: implement the restore fix in the TryBuild postfix
- [x] Implement in `BuildDoorOverWall/Mod.cs` inside `BuildTool_TryBuild_DoorReplacement__Patch.Postfix` (no new patch class, no new Harmony binding): BEFORE `def.TryReplaceTile(...)` capture `Grid.Objects[cell, (int)def.TileLayer]` for every door area cell (skip the step when `def.TileLayer == ObjectLayer.NumLayers` — covers Door/WoodenDoor); AFTER plan creation (`plan != null`) restore every captured non-null GO back into `Grid.Objects` and call `TileVisualizer.RefreshCell(cell, def.TileLayer, def.ReplacementLayer)` per affected cell (English comments with game file:line references, matching existing style).
- [x] Static check script `./.tmp/fix_door_wall_render_checks.sh`: asserts the capture+restore+refresh logic is present in the TryBuild postfix (capture before `TryReplaceTile`, restore guarded by `plan != null`, `RefreshCell` present, `NumLayers` skip present), existing mod invariants (single UserMod2, no `[HarmonyPatch]` attributes, no debug logging) and that `dotnet build ONI-mods.sln -c Debug` is green (0 CS errors; tolerated MSB3027/3021 into read-only ~/ONI).
- [x] Green: static check passes; full build green.
- [x] Refactor: keep the diff minimal; comments explain WHY (MarkArea overwrite, research §4.3) not what.

**Criterion:** static check passes; build green; the fix is confined to the existing TryBuild postfix body (door-def scoped via the existing `IsDoorDef` gate); no other file touched.
**Stage 2 outcome (journal):** implemented inside the existing `BuildTool_TryBuild_DoorReplacement__Patch.Postfix` (+39 lines, 19 code-only), exactly per trace §D: `NumLayers` guard; per-area-cell capture of `Grid.Objects[c, (int)def.TileLayer]` via the same orientation-aware `def.RunOnArea(__0, __instance.buildingOrientation, ...)` walk the candidate search uses — BEFORE `def.TryReplaceTile`; after `plan != null`, restore every non-null capture back into `Grid.Objects` and one `TileVisualizer.RefreshCell(cell, def.TileLayer, def.ReplacementLayer)` per restored cell; WHY-comments cite Constructable.cs:441-461, BuildingDef.cs:829-843 / :326-338 / :234-254, TileVisualizer.cs:23-33. New `.tmp/fix_door_wall_render_checks.sh` → PASS (ordering + invariants + build gate). Build: 0 CS errors (only tolerated MSB3027/3021), fresh repacked dll in bin/Debug/net48. Regression script `.tmp/fix_buld_door_checks.sh` fails ONLY on stale pre-revert assertions for `Constructable_MarkArea_DoorTileUnwind__Patch` (absent from the tree since 214854b) — unrelated to this fix, left unmodified. git diff: only `BuildDoorOverWall/Mod.cs` changed in the repo.
**Commit:** `<type>(<scope>): <summary>`

## Stage 3 — Acceptance: independent audit
- [x] Independent ralph audit (does not trust the implementer's report) against `.tmp/fix_door_wall_render_trace.md` + the new Mod.cs, checking: (1) broken orientation — after plan creation `Grid.Objects[upperWallCell, 9]` = the live wall GO again, `RefreshCell` fires, connection bits recover (no border insets); (2) cancel — wall intact, rendering intact (restored entry is the wall, `UnmarkArea` layer-11 only, no corruption left); (3) completion — wall destroyed exactly once, refund + replaced-trigger intact, finished door's `BuildingComplete.OnSpawn` re-marks both cells (self-heal preserved); (4) regressions — native orientations (whole-in-wall, lower-in-wall) untouched (postfix bails on existing plan; capture/restore is a no-op there), Door/WoodenDoor no-op (`TileLayer == NumLayers`), door-over-rock (no candidate → no plan → no capture), hover tint + 4-arg validity postfixes unchanged, no recursion/new Harmony binding risk; (5) build green, packed dll with one UserMod2.

**Criterion:** audit report (`.tmp/acceptance_fix_door_wall_render.md`) — all checks PASS or each FAIL justified and re-fixed before commit.
**Stage 3 outcome (journal):** independent ralph audit → **ACCEPT**, all 6 checklist items PASS with independent file:line evidence (`.tmp/acceptance_fix_door_wall_render.md`): (1) fixed state after plan creation — wall GO restored in the upper cell, plan owns the anchor slot, exactly one RefreshCell → `GetConnectionBits` reads the live wall → no border insets; (2) cancel leaves the wall intact (`UnmarkArea` layer-11 only, identity-guarded, no destroyed GO in a live slot); (3) completion — anchor candidate null (plan has no `BuildingComplete`), restored wall found at the upper cell → destroyed + refunded exactly once via Constructable.cs:235-254 with `ObjectReplaced` trigger, plan deleted once, finished door re-marks both layer-9 cells — no double-destroy/NRE/orphan; (4) all regressions verified (native orientations bail at the plan-exists guard BEFORE capture; Door/WoodenDoor `NumLayers` skip; door-over-rock returns at `candidate == null` before capture; cosmetic postfixes + `IsDoorDef` untouched; zero new `[HarmonyPatch]`/patch classes/Harmony bindings/logging; reentrancy guard intact); (5) build 0 CS errors, one UserMod2, checks PASS; (6) rotated (R90/horizontal) placements capture/restore the correct rotated footprint. **Observation (not a defect, out of scope):** for rotated placements the plan's own `MarkArea` runs pre-`SetOrientation` with Neutral orientation (BuildingLoader.cs:176-180, BuildingDef.cs:496-503) — a live wall in the unrotated neighbor (anchor+up) that is OUTSIDE the real footprint could still be grid-deregistered there; pre-existing vanilla corner, added to the in-game checklist as an observation item.
**Commit:** `<type>(<scope>): <summary>` (same commit as Stage 2 if no re-fix needed)

## Stage 4 — Finalization
- [x] Full review pass of the Mod.cs diff (naming, comments, no leftovers); confirm csproj/Directory.Build.* untouched (build invariants)
- [x] Fresh full build; confirm bin/ artifacts: repacked mod dll (UtilLibs+PLib packed), mod.yaml/staticID=BuildDoorOverWall
- [x] In-game verification checklist for the user (Russian) in the final report, covering: broken orientation × {Manual Airlock, Insulated Door, Mechanized Airlock} (no block borders), cancel → wall intact, complete build → door replaces wall + rendering normal, regression: Door/WoodenDoor same orientation, lower-in-wall, whole-in-wall, door-over-rock, hover color over wall/Backwall
- [x] Update plan journal; note hand-off to `fix_buld_door` (its Stage 3/4 remain deferred; its checks.md item "Требует доработки" can be closed by the user after in-game verification)

**Criterion:** clean full build; repacked dll in bin/; checklist delivered; all earlier-stage checkboxes [x].
**Stage 4 outcome (journal):** final review of the diff (single hunk inside the TryBuild postfix, +39 lines; comments clean, no leftovers; csproj/Directory.Build.* untouched); fresh build 0 CS errors; bin/ artifacts confirmed — repacked `BuildDoorOverWall.dll` in bin/Debug/net48 (PLib packed: 194 `PeterHan` / 216 `PLib` refs in the dll), `mod.yaml` `staticID: BuildDoorOverWall` present in bin/; Russian in-game checklist delivered in the final report. Hand-off to `fix_buld_door`: its Stage 3 (sandbox) / Stage 4 (finalization) remain deferred; its `checks.md` item «Требует доработки» can be closed by the user after in-game verification. Housekeeping note: `.tmp/fix_buld_door_checks.sh` is stale (still asserts the reverted `Constructable_MarkArea_DoorTileUnwind__Patch`) — fix or retire it within that task.
**Commit:** `<type>(<scope>): <summary>`
