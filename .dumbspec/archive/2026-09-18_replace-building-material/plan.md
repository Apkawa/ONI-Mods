# 2026-09-18_replace-building-material: implementation plan (ReplaceBuildingMaterial)

Spec — [spec.md](./spec.md); research — [research.md](./research.md).

**Format:** stages end with a commit; checkboxes are a **living progress journal** — update in place as work proceeds.
This repo has **no test suite** (only verification: `dotnet build` + user's manual in-game runs), so TDD is adapted: red = build without the piece / failing manual scenario, green = build passes + scenario works in-game.

Legend: `[ ]` not started · `[~]` in progress · `[x]` done.

---

## Stage 0 — Research + spec + plan
- [x] Raw input captured to `draft.md`; initial spec skeleton
- [x] Scope refined with user (clarification: no silent material swap — placement allowed, game logic demolishes/builds; buildings only)
- [x] Research subagent: BuildDoorOverWall mechanics, game placement validation, native replacement machinery, materials, project structure → `research.md`
- [x] Spec v1 written; open questions resolved with user
- [x] Plan written; task committed to freeze scope

**Criterion:** `draft.md`, `research.md`, `spec.md` (v1, no open questions), `plan.md` exist and are committed.
**Commit:** `chore(spec): spec and plan for ReplaceBuildingMaterial mod`

## Stage 1 — Deep research: same-layer replacement design
- [x] Read full code paths: `Constructable.OnSpawn`, `OnCompleteWork`, `FinishConstruction`, `PlaceDiggables`, `BuildingDef.TryPlace`/`Instantiate`/`IsAreaClear`/`GetReplacementCandidate`/`IsValidBuildLocation`, `BuildTool.TryBuild`/`InstantBuildReplace`
- [x] Decide the def-metadata configuration for building-over-same-pref-building (which `ReplacementLayer` value, `ReplacementCandidateLayers`, `ReplacementTags`, `Replaceable`) given plan and old occupant share the same ObjectLayer → D1 (research.md R8): `ReplacementLayer = ReplacementTile`, `ReplacementCandidateLayers = {Building}` (shared immutable), `ReplacementTags = {def.Tag}` (per-def), `Replaceable = true`, `EquivalentReplacementLayers` stays null
- [x] Decide the exact patch set (targets, prefix/postfix, incl. multi-cell area-aware fallback) → D3.1 (research.md R9): `Assets.AddBuildingDef` postfix (metadata, D2 scope predicate), **prefix** on `BuildTool.TryBuild(int)` (exact-overlap guard, pure rejection), **mandatory prefix** on `Constructable.FinishConstruction` (re-entry bail + pre-sweep slot clearing, N≥2), bidirectional `IsValidReplaceLocation` postfix (Stage 5 UX)
- [x] Record the decision in `research.md` (section R7) with code references; update spec Approach if it changed → R7 (deep trace), R8 (D1/D2), R9 (D3.1), R10 (verbatim validation bodies); spec Approach updated

**Criterion:** `research.md` contains a concrete metadata configuration + patch list, every decision backed by code references (class/method + line).
**Commit:** N/A (research notes committed with the first code commit)

## Stage 2 — Mod skeleton
- [x] Create `ReplaceBuildingMaterial/` folder: `ReplaceBuildingMaterial.csproj` (net48, `IsMod=true`, `IsPacked=true`, ProjectReference to `UtilLibs`), `Mod.cs` (`Mod : UserMod2` skeleton, `OnLoad(Harmony)`), `README.md`
- [x] Add project to `ONI-mods.sln` (GUID `{D75547EA-D35D-4217-97AE-348CC2E99918}`; `Project(...)` line + 4 configuration lines; no comments in sln)
- [x] `dotnet build` green (offline restore from `.cache/nuget/packages`); `bin/` contains `ReplaceBuildingMaterial.dll` + `mod.yaml`/`mod_info.yaml`

**Criterion:** Build succeeds; artifacts present in `bin/`; project listed in sln.
**Commit:** `feat(mod): add ReplaceBuildingMaterial project skeleton`

## Stage 3 — Replacement metadata injection
- [x] Implement scope filter: target "regular" building defs only (exclude defs with existing native replacement semantics — per Stage 1 decision)
- [x] Postfix on `Assets.AddBuildingDef`: set `Replaceable`/`ReplacementLayer`/`ReplacementCandidateLayers`/`ReplacementTags` per Stage 1 decision
- [x] `PUtil.LogDebug` under `#if DEBUG` for applied defs; warnings on real problems

**Criterion:** Build green; in-game (DEBUG log): targeted defs carry the metadata, non-targeted unchanged.
**Commit:** landed together with the Stage 4 commit `d9957c2` (same file `Mod.cs`, interleaved hunks — not split)

## Stage 4 — Placement validation gates
- [x] Gates: allow replacement only for same pref + exactly the same position (no rotation, no shift) + different material — `BuildTool_TryBuild_ReplaceBuildingMaterial__Patch` prefix (method is `void`; bool-returning short-circuit); same-pref enforced natively by per-def `ReplacementTags`; same-material no-op native (BuildTool.cs:366-371)
- [x] Area-aware multi-cell candidate check (native fallback inspects anchor cell only) — satisfied: exact overlap ⟺ clicked cell == old anchor cell, anchor always holds the candidate (R9-Q1)
- [x] Same material → no placement (red "occupied", behavior as without the mod)
- [x] MANDATORY completion fix-up: prefix on `Constructable.FinishConstruction` (R9-Q2.4, R11 row 3) — native completion is broken for N≥2 exact overlap (N re-entry fires, sweep NRE at SimCellOccupier.cs:176, ghost duplicates); re-entry bail on the private `finished` flag (Constructable.cs:51) + pre-sweep reference-matched clearing of the anchor candidate's ObjectLayer entries (door-mod template minus the distinct-candidate branch)
- [x] Build green

**Criterion:** Build green; in-game: replacement allowed exactly in the valid case (same pref/position, different material, incl. multi-cell); rotation/shift/same-material rejected.
**Commit:** `feat(mod): placement validation gates for building replacement`

## Stage 5 — Hover tint (UX)
- [x] Postfix on `IsValidReplaceLocation` for hover/preview tint — **bidirectional** (the native body is purely geometric, R10.1: it whitens same-material exact overlap, which the spec must show red "занято"); NOT the door mod's one-way flip. `__result = exact && !same` for mod defs (selectedElements via publicized `BuildTool.Instance?.selectedElements`)

**Criterion:** Build green; in-game: tint reflects validity.
**Commit:** `feat(mod): hover tint for replacement placement`

## Stage 6 — User in-game verification
- [x] User runs scenarios manually: (a) copper building over steel one, same position, multi-cell included — old demolished, items refunded, new built in copper; (b) same material — rejected as "occupied"; (c) rotated/shifted placement — rejected; (d) def with native replacement (door/foundation) — untouched by the mod
- [x] Fix reported issues, rebuild, re-verify

**Criterion:** User confirms all scenarios in (a)–(d) work as specced.
**Commit:** N/A (fixes committed as they land)

### Stage 6 journal

- **Debug round 1 (user report: big transformer — white ghost on cross-material, no plan placed, red "Must be built in unoccupied space" tooltip):**
  - Player.log analysis: only 5× `rejected drag` lines (MineralDeoxidizer, all cursor-on-non-anchor-cell — spec-correct rejections); ZERO PowerTransformer lines → on the transformer the prefix either passed through (exact overlap) or the click never hit a candidate cell; no exceptions.
  - Static re-trace of the entire native fallback (BuildTool.cs:350-377 + IsAreaClear verbatim) says the plan SHOULD spawn on exact overlap in a clean area → runtime state must be the culprit (e.g. a stale replacement plan in slot 11, or a slot-38/39 occupant) → instrumentation added:
    - TryBuild prefix exact branch: `LogFallbackInputs` replicates every native gate (ReplacementLayer, RunOnArea+IsReplacementLayerOccupied, slot-11 read, BuildingComplete/Replaceable/CanReplace, selectedElements count+material diff) and probes `IsValidPlaceLocation(replace_tile: true)` via the 5-arg overload (raw fail_reason, bypassing the hover postfix).
    - New TryBuild POSTFIX: after the original body, logs whether a Constructable landed in the def's ReplacementLayer slot at the drag cell (spawned vs silent failure).
    - Rejection log enriched: candidate anchor cell + candidate orientation + rotatable-null.
    - Cosmetic: postfix on the 4-arg `IsValidPlaceLocation(GameObject, Vector3, Orientation, out string)` (BuildToolHoverTextCard.cs:48-55 is the consumer) — blanks `fail_reason` only for valid cross-material exact overlap (bool untouched; same-material keeps the red "занято").
  - Build green; fresh DLL verified byte-identical in `~/ONI/mods/ReplaceBuildingMaterial_dev/` (post-build copy reached the game folder). Next: user re-test (same + cross material, aligned on anchor), then read Player.log for `exact drag` / `drag result` / `rejected drag` lines.
- **Round 7 (no new user run yet — Player.log last write 10:25, predates the 10:36 build):**
  - Verified the deployed DLL really contains the instrumentation: .NET string literals live in the `#US` heap as UTF-16LE, so ASCII grep reports 0 — Python UTF-16LE scan finds every new literal (`exact drag:`, `drag result:`, `probeReplaceValid`, `replacementSlot=`, `candidate anchor=`, plus the old ones). Deployment is confirmed; only a game launch is outstanding.
  - Ruled out the IsAreaClear slot-38/39 hypothesis for the user's map: ObjectLayer.AttachableBuilding is used only by OilWellCap/ReefGenerator/UnderwaterVentDrill and ObjectLayer.Gantry only by GantryConfig — none can sit in a regular building's footprint on a normal map.
  - Prime suspects for "white ghost, no plan, no log": (A) a stale replacement plan already in slot 11 blocking the native fallback via IsReplacementLayerOccupied (the tint stays white — it never checks slot 11; the first successful drag's plan is invisible-ish, same mesh position as the old building); (C) the click never reaching TryBuild. The static trace says the fallback MUST succeed in a clean area, so the new `exact drag` probe + `drag result` postfix will discriminate on the next test. No code change needed before the user's re-test.
- **Round 8 (still no new user run — log unchanged) — full static audit of the N≥2 FinishConstruction completion path (the largest untested code block), line-by-line against decompiled sources. NO BUGS FOUND:**
  1. Plan grid registration: `TryReplaceTile` (BuildingDef.cs:487-506) sets `IsReplacementTile = true` on the PREFAB's Constructable (:493) BEFORE `Instantiate` — the KInstantiate clone inherits the flag (the prefab flag is reset at :495, the instance keeps it). `Constructable.MarkArea` (:441-447) marks `IsReplacementTile ? ReplacementLayer : ObjectLayer` → slot 11 at ALL footprint cells via `BuildingDef.MarkArea` (iterates all rotated PlacementOffsets). `KInstantiate`'s 5th arg (`layer` from TryReplaceTile, default 0) is the Unity `gameLayer` (GameUtil.cs:2660) — render layer, no grid involvement.
  2. Completion trigger: `OnCompleteWork` anchor branch (:158-221) — `IsReplacementTile` true on the instance → `GetReplacementCandidate(anchorCell)` = old building (slot-1 entries still live) → `SimCellOccupier.DestroySelf(callback)` (SimCellOccupier.cs:143-181): for an air-occupying building the `doReplaceElement` branch is never taken (cell element is Vacuum, not the building's primary element) → else branch per cell: `Grid.SetSolid(false)` + `onComplete.Signal()` → the callback fires synchronously N times (once per footprint cell) INSIDE the DestroySelf call, before :197-213 (refund/Trigger/old DeleteObject) run.
  3. Fire 1 through the prefix: `finished` still false → proceed; N>1 → prefix clears the anchor candidate's (old building's) ObjectLayer entries reference-matched over its whole `PlacementCells` BEFORE the original body → the sweep (:228-257) `GetReplacementCandidate` at every non-anchor cell returns null → sweep no-ops (no NRE, no double refund, no double DeleteObject). Original continues: `UnmarkArea` (Constructable.cs:463-477) unmarks the plan's slot-11 entries over the full footprint (same `IsReplacementTile ? ReplacementLayer : ObjectLayer` logic, reference-matched) → `Def.Build` (BuildingDef.cs:397-452) creates the new building and `MarkArea(cell, orientation, ObjectLayer, go)` marks it in slot 1 at all N cells (overwriting the old building's entries) → `finished = true` (:289) → plan `DeleteObject` (:290, deferred to end of frame).
  4. Fires 2..N (remaining `onComplete.Signal()` iterations of the still-running DestroySelf loop): prefix reads `finished == true` → bail, original skipped — no ghost duplicates.
  5. Old building teardown ordering: refund `SpawnItemsFromConstruction` + replace-event Trigger + `DeleteObject` (:202-213) run after fire 1 (deferred destroy); at end of frame the old building's `OnCleanUp` unmark runs reference-matched — entries already cleared (prefix) or overwritten (Def.Build) → no-ops, no stale grid state.
  6. The `BuildingComplete.Subscribe(FinishConstruction)` path (:183-190) is only reached when the candidate has NO SimCellOccupier — never true for a real old building → no second completion source.
  7. 1x1 plans: sweep gate false (N=1), exactly one callback fire — native path is already correct, prefix is a no-op (only the re-entry guard, which never trips).
  8. Also re-verified the D2 scope predicate (`IsRegularBuildingDef`, Mod.cs:231-243) for scenario (d) structural protection: foundations/walls excluded by `TileLayer`/`ObjectLayer` clauses, conduits/attachables/gantries by `ObjectLayer == Building`, native-replacement defs by `ReplacementLayer == NumLayers && ReplacementTags == null`, doors by `IsDoorDef` (door component or `CopyBuildingSettings.copyGroupTag == GameTags.Door`). Scenario (d) holds structurally.
  Conclusion: completion path is sound; no code change. Waiting on the user's instrumented-build re-test to name the drag-time failing gate.
- **Round 9 (still no new user run — Player.log unchanged, 1079 lines) — closed the two remaining diagnostic gaps so the next test run is 100% conclusive for every outcome. Two DEBUG-only, read-only additions (no behavioral change):**
  1. `BuildTool.TryBuild` prefix pass-through branch (previously silent): now logs `pass-through drag: def={0} cell={1} candidate={2} canReplace={3}` — names the "no candidate at all" vs "candidate but native CanReplace false" sub-cases (e.g. old building mid-deconstruction would show candidate=null).
  2. New DEBUG postfix on `BuildTool.OnDragTool(int, int)` (protected override, BuildTool.cs:302-305): `drag entry: def={0} cell={1} dist={2}` — proves a world click/drag actually reached the build tool, closing hypothesis C ("click never hit TryBuild"). Wiring added in OnLoad via the same FindMethod + programmatic `harmony.Patch` pattern.
  - Traced while instrumenting: `OnDragTool` calls `TryBuild(cell)` unconditionally; TryBuild's dedup guard (BuildTool.cs:309, same cell+orientation as the previous drag → early return) still gets full instrumentation because the prefix runs BEFORE the guard and the postfix runs AFTER the original body (even on the early return) — no lost events.
  - Build green (0 errors; known CS8600/8603/8604/8618-style nullability warnings only).
  - Deployment: the post-build `CopyModsToDevFolder` MSBuild target updated the game folder this round (deployed DLL mtime 11:13:03.153, 53 ms after the 11:13:03.099 build; a manual `cp` from the sandbox shell still fails read-only, as AGENTS.md expects). UTF-16LE scan of the deployed DLL: all five literals present (`drag entry:`, `pass-through drag:`, `OnDragTool` ×2, `exact drag:`, `drag result:`); mod folder clean (single dll+pdb+yaml set).
  - Decision matrix for the next user test (every outcome is now named by the log): no `drag entry` → click never reached the build tool (UI swallowed it / different panel); `drag entry` + `pass-through drag: candidate=null` → old building has no slot-1 entry (mid-deconstruction or already replaced); `pass-through drag: canReplace=False` → tag mismatch; `exact drag` + `drag result: replacementSlot=null` → native fallback failed silently (the `exact drag` line's `probeReplaceValid/probeReason` fields name the exact gate); `exact drag` + `drag result: isConstructable=True` → plan spawned; a second drag on the same cell then shows `slot11Occupied=True` (hypothesis A: stale plan blocks repeats).
- **Round 10 (final round — no new user run; Player.log still 1079 lines, mtime Sep 19 10:25, predating every instrumented build):**
  - No in-game launch occurred; all work that is possible without a game run is COMPLETE: Stages 1–5 committed; Stage 6 debug build deployed and verified byte-identical in the game mod folder (round-9 `9eeb25c` DLL, UTF-16 scan shows all five instrumentation literals); completion path audited line-by-line (round 8, no bugs); D2 scope verified (scenario d structurally protected); every silent diagnostic gap closed (rounds 7, 9).
  - Remaining work is strictly user-gated: (1) user runs the game with the deployed DEBUG build and repeats the transformer test (same-material red ghost; cross-material exact overlap → white ghost + click); (2) report outcome; (3) read `drag entry` / `exact drag` / `drag result` / `pass-through drag` / `rejected drag` lines in Player.log per the round-9 decision matrix; (4) apply the targeted fix, rebuild, verify re-deployment; (5) user re-verifies scenarios (a)–(d).
  - Per AGENTS.md, acceptance is performed by the user via manual in-game runs — no automated game run exists in this sandbox, and the game itself runs on the user's machine.
  - BLOCKED (3+ consecutive rounds, 7–10, on the same concrete condition): awaiting the user's in-game run of the instrumented build.
- **Round 11 (user ran the instrumented build — "poked the transformer, plan did not appear"): ROOT CAUSE FOUND AND FIXED:**
  - Player.log (974 lines, session 08:37:14–08:37:17, three attempts at cells 153872/153869/153875) per the round-9 decision matrix:
    - `drag entry: def=PowerTransformer cell=153872 dist=0` → the click reached the build tool (hypothesis C is dead).
    - `exact drag:` — candidate=PowerTransformerComplete, exact anchor match, `ReplacementLayer=ReplacementTile`, **`replacementLayerOccupied=False slot11Occupied=False`** (hypothesis A — stale slot-11 plan — is dead), `BuildingComplete=True Replaceable=True CanReplace=True`, `selectedCount=1 selected0=Copper candidateTag=Cobalt materialDiffers=True` (Copper over Cobalt — the intended cross-material case).
    - **The gate:** `probeReplaceValid=False probeReason=Разъемы электропитания не могут совпадать` (= `STRINGS.UI.TOOLTIPS.HELP_BUILDLOCATION_WIRECONNECTORS_OVERLAP`, "Power connectors cannot overlap"). `drag result: replacementSlot=null` — the native fallback failed silently, exactly as the matrix predicted.
  - Root cause (source audit): 6-arg `IsValidPlaceLocation` → `IsAreaClear` with `replace_tile: true` — the main-loop occupancy check already EXCLUDES the replacement candidate (BuildingDef.cs:602), so it passes; the tail `&& ArePowerPortsInValidPositions(source_go, cell, orientation, out fail_reason)` (BuildingDef.cs:789) then reads `Grid.Objects[portCell, 29]` (`ObjectLayer.WireConnectors`) and rejects ANY occupant that is not `source_go`. `BuildTool.TryBuild` passes the ghost PREFAB as `source_go` (BuildTool.cs:376), while the OLD building registered its own port entries at layer 29 via `MarkArea` (BuildingDef.cs:861-874, `PowerInputOffset`/`PowerOutputOffset`) → for ANY building with power ports, the native replacement path can never pass. (Door-over-wall / foundation-over-foundation never hit this — they have no power ports.)
  - Fix (implemented this round, two programmatic POSTFIXes on the private checks, identical `(GameObject, int, Orientation, out string)` signature):
    1. `BuildingDef_ArePowerPortsInValidPositions_ReplaceBuildingMaterial__Patch` — lifts the verdict when `fail_reason == WIRECONNECTORS_OVERLAP` AND it is a mod-def cross-material exact overlap (`GetReplacementOverlapCandidate`: IsModDef + IsExactOverlap + NOT IsSameMaterial — the same-material red "занято" stays native) AND every new-building power-port cell (rotated `PowerInputOffset`/`PowerOutputOffset` where `RequiresPowerInput`/`RequiresPowerOutput`) is empty or owned by the candidate's own entry (`PowerPortEntriesOwnedBy`) — i.e. a wire bridge or another building still blocks natively.
    2. `BuildingDef_AreConduitPortsInValidPositions_ReplaceBuildingMaterial__Patch` — same pattern for gas/liquid/solid conduit buildings (`fail_reason` ∈ {GASPORTS/LIQUIDPORTS/SOLIDPORTS}_OVERLAP; primary ports via rotated `UtilityInputOffset`/`UtilityOutputOffset`, layer from `Grid.GetObjectLayerForConduitType` — the same writer/checker pair as native `MarkArea`/`IsValidConduitConnection`, BuildingDef.cs:1423-1487/1621+). Secondary I/O ports stay natively rejected (rare on regular buildings, documented).
  - Why only the port checks needed touching: logic ports (`AreLogicPortsInValidPositions`, BuildingDef.cs:1558-1586) are scene-based (`logicCircuitManager.GetVisElements()` vs the source's LogicPorts/LogicGateBase components) — the prefab ghost has no such components, so it can never fail there. Hover-card (4-arg) and `TryPlace` paths fail on occupancy long before the port checks → unaffected; the existing 4-arg fail_reason postfix still drives the hover card.
  - Build green (0 errors; same known CS8600/8603/8604/8618/8625 nullability warning class, 25 total).
  - Deployment: post-build `CopyModsToDevFolder` updated the game folder (344576 bytes, byte-identical to bin/, deployed mtime 12:09:37.844 vs build 12:09:37.753). UTF-16LE scan of the deployed DLL: both new literals present (`power port overlap allowed for replacement:`, `conduit port overlap allowed for replacement:`), both method names present.
  - Next: user re-runs the same transformer test (Copper over Cobalt, exact position). Expected new log lines: `power port overlap allowed for replacement: ...`, `exact drag: ... probeReplaceValid=True`, `drag result: ... isConstructable=True` → the plan spawns; construction then runs the audited completion path (round 8) to finish the replacement.
- **Round 12 (clean user session, log at .tmp/Player_1.log — 998 lines, session 09:24:17–09:25:48): power fix CONFIRMED WORKING; a SECOND gate exposed and fixed:**
  - Evidence from the log:
    - Cross-material drags (Copper over Cobalt, cells 153869/153872/153875, 09:25:39–41): `power port overlap allowed for replacement: def=PowerTransformer ... candidate=PowerTransformerComplete` fires (twice per drag — native call chain + my 5-arg probe) → the round-11 power-port fix works.
    - Same-material drags (Cobalt over Cobalt, 09:25:45–48): the postfix correctly does NOT fire (`materialDiffers=False`) and the native rejection stands (`probeReason=Разъемы электропитания не могут совпадать`) → the spec's red "занято" for same material is preserved. ✓
    - BUT cross-material still fails: `probeReplaceValid=False probeReason=Порты автоматизации не могут совпадать` (= `STRINGS.UI.TOOLTIPS.HELP_BUILDLOCATION_LOGIC_PORTS_OBSTRUCTED`, "Automation ports cannot overlap") and `drag result: replacementSlot=null` — the NEXT gate in the IsAreaClear &&-chain (BuildingDef.cs:789: power && conduit && logic).
  - Root cause of the logic-port gate (round-10 assumption "prefab has no logic ports → can never fail" was WRONG): the check (BuildingDef.cs:1558-1586) is scene-based — `logicCircuitManager.GetVisElements()` contains BOTH the old building's physical port elements (registered at spawn, `LogicPorts.CreatePhysicalPorts` → `LogicEventSender`/`LogicEventHandler`, LogicPorts.cs:221/256) AND the ghost's own visualizers — registered by the native check's own `HackRefreshVisualizers()` call (`CreateVisualizers` → `LogicPortVisualizer`, LogicPorts.cs:163/174). Same def + anchor + orientation ⇒ identical port cells ⇒ the ghost ALWAYS conflicts with the candidate's own ports.
  - Fix: third POSTFIX on `BuildingDef.AreLogicPortsInValidPositions(GameObject, int, out string)` (private; note: NO orientation parameter — the ghost's own `Rotatable` carries the drag orientation). Same strict gates (mod def + exact overlap + cross-material) plus a per-cell OWNERSHIP PROOF: vis-elements carry no owner reference, so counting is used — at every ghost port cell, the scene elements that are not the ghost's own must be no more than the candidate's own port count at that cell; any surplus element (another gate or building) keeps the native rejection. Null-list guards on the port lists (component may exist without configured ports).
  - Build green (0 errors; same known nullability warning class, 26 total). Deployment verified byte-identical in the game mod folder (347136 bytes); UTF-16LE scan: `logic port overlap allowed for replacement:` present, all three port literals + both method names present.
  - Next: user re-runs the transformer test (cross-material exact overlap). Expected: `power port overlap allowed ...` + `logic port overlap allowed ...` lines, `probeReplaceValid=True`, `drag result: isConstructable=True` → plan spawns → construction runs the audited completion path (round 8).
- **Round 13 (user report: replacement WORKS — plan created, transformer replaced successfully — but with a leftover error): the "overlapping ports" tag bug found and fixed.**
  - User report: during construction the plan showed the error tooltip "Ошибка: совпадение портов" plus the global notification "У этой постройки есть совпадающие порты"; and if the plan is CANCELLED, the error stays and the old transformer stops working.
  - Root cause: `BuildingDef.MarkOverlappingPorts(GameObject existing, GameObject replaced)` (BuildingDef.cs:941-954), called from `MarkArea` (:850-934) for EVERY port cell the marking building claims (conduit layers, power layer 29 at :865/:872, wire bridges, secondary conduit). The replacement plan's MarkArea runs while the old building is still in the world, so the old building's own power-port cells read back the OLD building as occupant → `existing.AddTag(GameTags.HasInvalidPorts)`. That tag drives `InvalidPortReporter` (InvalidPortReporter.cs:22-35): it flips the Operational flag `ports_not_overlapping` OFF (Type.**Functional** → the old building is DISABLED — "stops working") and toggles the `InvalidPortOverlap` status item + notification (Db.cs:1618-1619, STRINGS.BUILDING "Building has overlapping ports"). The tag never clears natively (only a re-mark of that cell with an empty occupant would clear it — which never happens in this flow), so cancelling the plan leaves the old transformer permanently tagged/broken.
  - Fix (two parts):
    1. PREFIX on `BuildingDef.MarkOverlappingPorts(GameObject, GameObject)`: for the replacement pair only (a Constructable plan of a mod def at exactly the anchor cell + orientation of an already-complete building — a pair the native game can never produce), skip the original so the old building is never tagged. Every other overlap keeps the native tag.
    2. STALE-TAG HEAL in the TryBuild prefix (exact-overlap branch, before the plan spawns): if the candidate still carries `HasInvalidPorts` from a pre-fix build, `RemoveTag` it — heals existing saves; the tag can never be a genuine third-party overlap (only this mod's flow ever marks over the candidate's own port cells).
  - Note: at construction COMPLETION the new building's MarkArea still sees the dying plan's go in the port cells and tags THAT — harmless (the plan is destroyed the same frame).
  - Build green (0 errors, same 26 known nullability warnings). Deployment verified byte-identical in the game mod folder (348160 bytes); UTF-16LE scan: both new literals + `MarkOverlappingPorts`/`HasInvalidPorts` present.
  - Next: user re-runs the transformer test. Expected: no "overlapping ports" error/notification during construction, old transformer keeps working while the new one is built; cancel case: old transformer unharmed; complete case: clean replacement. If the old (already tagged in the current save) transformer is the test target, the heal line `cleared stale HasInvalidPorts tag ...` should appear in the log.
- **Round 14 (user report: transformer/autoplant/thermoregulator "work perfectly"; pumps, generators (coal, gas), batteries "don't work" — mode unspecified, no new log): static audit, no code differentiator found.**
  - Failing configs audited in full: LiquidPump 2×2 `Anywhere` (power input @ (0,1), liquid OUTPUT only @ (1,1), logic input port @ (0,1), ConduitDispenser); Generator 3×3 `OnFloor` (power output @ (0,0) = anchor cell, logic input @ (0,0), coal via ManualDeliveryKG, no conduits); MethaneGenerator 4×3 `OnFloor`; PetroleumGenerator 3×4 `OnFloor`; Battery 1×2 `OnFloor` (BaseBatteryConfig: `RequiresPowerOutput` default offset, PoweredActiveController → logic port).
  - Key structural difference vs the working set: **all failing buildings have LOGIC ports** (LogicOperationalController / PoweredActiveController). The working transformer also has one (round-12 gate), so the round-12 logic-port fix is what must carry these — its per-cell counting proof is def-agnostic. Ghost `inputPortInfo` is populated at prefab generation from `def.LogicInputPorts` (GeneratedBuildings.cs:109) — the mod def's clone inherits it.
  - Secondary-conduit gap CONFIRMED NON-ISSUE: no failing def adds `ConduitSecondaryInput`/`ConduitSecondaryOutput` (or any ISecondaryInput/ISecondaryOutput component) — `ConduitDispenser`/`ConduitConsumer` merely reference the interfaces internally. The documented primary-only coverage of `ConduitPortEntriesOwnedBy` is sufficient for these defs.
  - Completion path re-verified: `FinishConstruction` pre-sweep clears ONLY slot-1 (Building layer) entries reference-matched against the dying old building — conduit/wire/port layers untouched; post-completion utility wiring is fully native.
  - Conclusion: no def-specific gate exists in the placement chain (all my gates are def-agnostic); the differentiator must be in the data — the failure mode + the DEBUG log's `exact drag:` `probeReplaceValid/probeReason` lines will localize it.
  - BLOCKED on user data: (1) the exact failure mode (red ghost / no plan vs plan created but new building non-functional vs error shown), (2) a fresh session log copied to `.tmp/Player_3.log`.
  - **DATA RECEIVED — ROOT CAUSE FOUND in `.tmp/Player_3.log` (992 lines, session 14:22:33–14:24:20).** Failure mode confirmed by user: red ghost, no plan, click does nothing. Log: 8 rejected drags — LiquidPump ×4 @ anchor 157494, GasPump ×4 @ 101797 — every one `rejected drag: ... candidate anchor=<same cell> candidateOrientation=? rotatableNull=True`. The anchor cells MATCH; only the orientation comparison is impossible, because **the old pumps have NO `Rotatable` component**. (No generator/battery drags in this log — those tests predate it; their configs are unrotatable by default too, so the same cause is expected.)
  - Why: `BuildingLoader.cs:176` adds the `Rotatable` component to a building prefab only when `def.PermittedRotations != PermittedRotations.Unrotatable` (`PermittedRotations.Unrotatable == 0` is the enum default). Pumps/generators/batteries never set `PermittedRotations` → unrotatable → no component. The working transformer sets `PermittedRotations.FlipH` (PowerTransformerConfig.cs:23) — that is why it worked while the unrotatable set failed. The game's own convention for orientation-less buildings is `Orientation.Neutral` (`Building.cs:30-40`, `Building.Orientation` property) and every native placement path null-guards the component (`BuildingDef.Build`:408-420, `TryPlace`/`TryReplaceTile`:454-527, `MarkArea` uses the static `Rotatable.GetRotatedCellOffset(offset, orientation)` with an explicit orientation param, `LogicPorts.GetActualCell`/`CreateVisualizers` null-guard). ONLY my mod hard-required it, in exactly three spots.
  - Fix (implemented, round 14, fix part 1): (a) `IsExactOverlap` — a candidate WITHOUT `Rotatable` skips the orientation comparison; anchor equality alone decides exactness (rotation is impossible for such defs, so "no rotation/shift" is satisfied by the anchor check). (b) `AreLogicPortsInValidPositions` postfix — the signature has no orientation param, so a rotatable-less ghost falls back to `Orientation.Neutral` instead of the old early return (the power/conduit postfixes take orientation as a native parameter and needed no change). (c) `IsReplacementPair` (round-13 `MarkOverlappingPorts` prefix) — an unrotatable pair (both sides rotatable-less) compares the anchor only; without this, the round-13 cancel-corruption (stale `HasInvalidPorts` tag disabling the old building) would recur for every unrotatable replacement.
  - Mixed rotatability is impossible: `CanReplace`/`GetReplacementCandidate` only ever pair SAME-pref objects, and same pref ⇒ same `PermittedRotations`.
  - Build + user re-test pending: pumps (liquid + gas) should now replace with a different material; retest generators (coal, gas, petroleum) and batteries too — expected same cause (config-level unrotatable), but their failure was observed in an earlier session, so a fresh log is needed only if any of them still fails.
- **Round 14b (user report: "already better, white ghost, no errors — but the replacement plan is not created"; fresh log at `.tmp/Player_4.log`, session 15:11:49–15:14:51): round-14 fix part 1b never reached the code — found and fixed.**
  - Log evidence (pump drag @ anchor 157494, 15:14:05; kiln drags @ 99989/165559, 15:13:14–15:14:51 — kilns are unrotatable with a logic port too, same symptom):
    - `exact drag: def=LiquidPump ... candidate=LiquidPumpComplete ... replacementLayerOccupied=False ... Replaceable=True CanReplace=True ... materialDiffers=True probeReplaceValid=False probeReason=Порты автоматизации не могут совпадать` — the exact-overlap branch now fires (round-14 part 1a works: white ghost, candidate resolved) and every gate PASSES except the last one in the :789 chain.
    - `power port overlap allowed for replacement` and `conduit port overlap allowed for replacement` both fire — the round-11 power/conduit postfixes lift correctly for unrotatable defs (they take orientation as a native parameter; no change needed).
    - `logic port overlap allowed for replacement` is ABSENT, and `probeReason` = `STRINGS.UI.TOOLTIPS.HELP_BUILDLOCATION_LOGIC_PORTS_OBSTRUCTED` ("Automation ports cannot overlap") — the round-12 logic postfix is the sole remaining blocker.
    - `drag result: replacementSlot=null isConstructable=False` → no plan. All other fallback gates green (`replacementLayerOccupied=False slot11Occupied=False BuildingComplete=True Replaceable=True CanReplace=True`).
  - Root cause — my own earlier slip: the round-14 edit of the logic postfix updated the DOC COMMENT ("with the round-14 Neutral fallback...") but the code BODY still contained the original early return `if (rotatable == null) return;`. A pump ghost has no `Rotatable` (round-14 part 1a context) → the postfix silently bailed before even reaching `GetReplacementOverlapCandidate` → the native logic-port rejection stood. Part 1a (`IsExactOverlap`) and part 1c (`IsReplacementPair`) had their code actually changed, which is why the ghost tint and candidate resolution recovered while the plan creation did not.
  - Fix (implemented, round 14b): the logic postfix now computes `Orientation ghostOrientation = rotatable != null ? rotatable.GetOrientation() : Orientation.Neutral` and proceeds to the same strict gates (mod def + exact overlap + cross-material + per-cell ownership proof in `LogicPortElementsOwnedBy`) — exactly what the doc comment already promised. No other change; the ownership count was audited against the live pump scene state (scene[A] = old pump's `LogicEventHandler` + ghost `LogicPortVisualizer`, own[A] = 1, candidate[A] = 1 → passes).
  - Build + deploy + user re-test pending: expected log lines for the pump drag: `logic port overlap allowed for replacement: def=LiquidPump ... candidate=LiquidPumpComplete`, `exact drag: ... probeReplaceValid=True`, `drag result: ... isConstructable=True` → plan spawns; construction then runs the audited completion path (round 8). Generators/batteries (same unrotatable + logic-port profile) should now work too.
- **Round 14c — ACCEPTANCE (user: "Потыкал все, пока багов не заметил. все хорошо работает"): Stage 6 COMPLETE.**
  - Scenario (a): confirmed across the full tested set — transformers, autoplants, thermoregulators (rotatable) and liquid/gas pumps, coal/gas/petroleum generators, batteries, kilns (unrotatable, logic-port), including multi-cell footprints (2×2, 3×3, 4×3, 3×4, 1×2): plan created, old building demolished, materials refunded, new building built in the selected material.
  - Scenario (b): confirmed since round 12 — same-material exact overlap stays red "занято" (`materialDiffers=False`, postfix correctly does not fire).
  - Scenario (c): confirmed since rounds 1/14a — shifted drags hit the `rejected drag:` branch (anchor mismatch), no rotation possible for these defs.
  - Scenario (d): structurally protected (D2 scope predicate audit, round 8) — defs with native replacement semantics (doors, foundations, stairs, windows, attachables, gantries, conduits) are never touched; no regression reported by the user.
  - Cancel-safety (round 13 fix): no "overlapping ports" error/notification during or after construction, cancel leaves the old building working — no counter-examples in the user's session.
  - Follow-up requested by the user: mod `ReplaceBuildingMaterial/README.md` written (features, usage, limitations, build) + the mod added to the root `README.md` table.
